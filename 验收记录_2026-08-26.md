# 网页链路验收记录（2026-08-26）

## 环境

- 后端：`http://127.0.0.1:8000`（FastAPI/Uvicorn，已启动）
- 页面：`/simulation-demo`，HTTP 200
- 页面脚本固定 `const userId='10001'`，聊天请求使用 `user_id:Number(userId)`，时间轴请求使用同一值。

## 验证结果

1. 时间轴读回：`GET /api/v1/life-events?user_id=10001&days=30` 返回 200，CORS 响应头包含 `access-control-allow-origin: http://localhost:5173`，返回 `count: 27`，数据中的 `user_id` 均为 `10001`。
2. 聊天写入：页面发送 `POST /api/v1/chat` 时请求体包含 `user_id=10001`。本次真实 Agent 请求返回 HTTP 400（`INVALID_REQUEST`），因此未伪造成功消息；页面 catch 分支会显示错误文本。
3. Reflection / Planning：两者由 `/api/v1/chat` 响应中的 `reflection_result` 与 `generated_plan` 字段承载；本次后端未返回 2xx，前端不会显示模拟成功结果，符合错误处理验收项。
4. CORS：已实测 `Origin: http://localhost:5173`，响应允许该来源。
5. 非 2xx 防伪成功：`simulation-demo.html` 明确执行 `if(!r.ok) throw Error(...)`，只在 2xx 时插入 `assistant_response`，错误时插入 `.msg.bot.error`。

原始 HTTP 请求/响应已保存于 [`验收证据_2026-08-26.txt`](./验收证据_2026-08-26.txt)。
