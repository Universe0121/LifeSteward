# 成员三 2026-08-26 今日开发任务

负责人：成员三  
角色：Production API / Agent Workflow / E2E Owner  
基线：GitHub `origin/main@361cc37`

## 今日唯一 P0 目标

在成员二提供真实 DSN/key 后，完成正式 API、Agent 工作流和真实 E2E 收口，并为成员一的网页联通提供稳定接口。

## 一、10:30 前：后端回归和 planning 测试补齐

- 运行 GitHub 当前完整测试，记录 `90 tests` 基线。
- 检查 `backend/tests/test_master_agent_routing.py`：planning 路由当前存在空壳 `pass` 测试，必须补成真实断言。
- 断言必须覆盖：
  - `MemoryAgent -> PlanningAgent -> InteractionAgent` 顺序。
  - `generated_plan` 包含 `task_name`、`start_time`、`duration_minutes`、`difficulty`。
  - InteractionAgent 收到并生成最终用户响应。
- 不改变已冻结 `AgentState` 字段和公共 Chat API 字段。

## 二、13:00 前：正式 API 和错误语义收口

确认并测试：

- `GET /api/v1/life-events?user_id=10001&days=7` 正常、空结果、非法参数、Tool 异常均有明确行为。
- `record_event` 持久化失败返回标准错误，InteractionAgent 不执行，不出现成功文案。
- 真实数据库不可用时返回可定位的错误，不自动回退 `InMemoryMemoryService`。
- `/api/v1/chat`、`/api/v1/life-events` 都使用启动时共享的 production composition root。

如成员一发现字段问题，优先在 schema/API 适配，不让前端直接依赖数据库内部实现。

## 三、15:00 前：运行真实 Reflection/Planning E2E

等待成员二确认 DSN/key 后运行：

```powershell
cd backend
$env:LIFE_STEWARD_E2E = "1"
python -m unittest tests.test_e2e_production_smoke -v
```

必须证明：

- chat record 成功。
- `GET /api/v1/life-events` 读回成功。
- Reflection 返回 `intent=reflection`，检索记忆来自真实 pgvector。
- Planning 返回 `intent=planning`，计划字段完整。
- 真实 embedding 已使用；deterministic fallback 只能单独标为本地调试结果。

## 四、16:30 前：与成员一完成网页联调

- 提供 Chat API 和 life-events API 的稳定请求/响应样例。
- 协助成员一确认后端启动、代理、CORS/错误响应和用户 ID 一致。
- 用浏览器完成：聊天写入 → 时间轴回读 → Reflection → Planning。
- 如前端收到 500，确认错误响应不会被前端伪装成成功。

## 五、18:00 前：测试与 handoff

运行：

```powershell
cd backend
python -m unittest discover -s tests -p "test_*.py" -v
```

更新 `成员三/day06_handoff_03.md`，记录：

- 分支和 commit。
- API/schema/service 修改。
- planning 路由测试补齐情况。
- 持久化失败语义。
- 真实数据库、embedding、Reflection、Planning 和浏览器联调结果。
- 测试总数、跳过项及其原因。

## 今日禁止事项

- 不新增 Agent、AgentState 字段、模型提供商或数据库 Schema。
- 不为了测试全绿而删除真实环境 Gate。
- 不把 deterministic fallback 冒充 DashScope 真实验收。
- 不扩展 Profile、DIY、首页任务、Redis、通知或移动端。

## 完成标准

成员三只有在 API 测试、planning 路由断言、真实 Reflection/Planning E2E 和成员一浏览器联通全部通过后，今日任务才算完成。


10:30 前：后端回归与 Planning 测试

运行完整测试，记录 90 tests 基线。
补齐 test_master_agent_routing.py 的 planning 路由断言。
验证流程：MemoryAgent -> PlanningAgent -> InteractionAgent。
验证计划字段和最终用户响应。


13:00 前：正式 API 与错误语义

测试 GET /api/v1/life-events 的正常、空结果、非法参数和 Tool 异常。
验证 record_event 持久化失败时返回标准错误，且不执行 InteractionAgent。
确认真实数据库不可用时不回退内存服务。
确认 Chat 和 life-events 共用 production composition root。
15:00 前：真实 Reflection/Planning E2E

等成员二提供真实 DSN/key。
运行 tests.test_e2e_production_smoke。
验证聊天写入、时间轴回读、Reflection、Planning、真实 pgvector 检索和 embedding。
明确区分真实 embedding 与 deterministic fallback。
16:30 前：网页联调

提供 Chat API 和 life-events API 请求/响应样例。
协助确认启动、代理、CORS、错误响应和用户 ID。
浏览器验证：聊天写入 → 时间轴回读 → Reflection → Planning。
确认 500 错误不会被前端伪装成成功。
18:00 前：测试与交接

运行完整测试：
python -m unittest discover -s tests -p "test_*.py" -v
更新 成员三/day06_handoff_03.md。
记录分支、commit、API/schema/service 修改、测试结果、跳过项及原因。
**明确不做：**新增 Agent、AgentState 字段、模型提供商、数据库 Schema，以及 Profile、DIY、首页任务、Redis、通知和移动端扩展。

当前关键依赖：成员二提供真实数据库 DSN 和 key；没有真实配置时只能完成本地回归，不能宣称真实 E2E 验收完成。

## 网页联调冻结说明

统一用户 ID：`10001`；前端开发地址：`http://localhost:5173`；后端地址：`http://localhost:8000`。Vite 将 `/api/*` 代理到后端，后端同时允许 localhost/127.0.0.1:5173 的 CORS。

Chat API 请求示例：

```json
POST /api/v1/chat
{"user_id":10001,"conversation_id":"web-demo-20260826","user_input":"今天学习数学2小时，有点累"}
```

成功响应示例：

```json
{"assistant_response":"已记录。","intent":"record_event","extracted_events":[{"event_type":"study","event_content":"今天学习数学2小时，有点累"}]}
```

时间轴回读：`GET /api/v1/life-events?user_id=10001&days=7`

```json
{"items":[{"life_event_id":1,"user_id":"10001","conversation_id":"web-demo-20260826","event_type":"study","event_content":"今天学习数学2小时，有点累","event_time":null,"emotion":"tired","importance_score":0.7,"source":"text","source_text":"今天学习数学2小时，有点累","created_at":"2026-08-26T10:00:00+08:00"}],"count":1}
```

Reflection 与 Planning 仍通过同一个 Chat API，分别发送“最近为什么学习效率下降？”和“根据我最近的状态，帮我安排明天的学习计划”，验收 `intent` 分别为 `reflection`、`planning`。

错误约定：参数错误返回 HTTP 400、`{"success":false,"error_code":"INVALID_REQUEST",...}`；服务/数据库错误返回 HTTP 500、`error_code` 为 `AGENT_PROCESSING_ERROR` 或 `INTERNAL_SERVER_ERROR`。前端收到非 2xx 时显示“请求失败”并标记“未写入时间轴”，不会再追加 mock 成功回复。

## 2026-08-26 完整后端回归最终结果

执行命令：

```powershell
cd backend
$env:LIFE_STEWARD_E2E = ""
..\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v
```

执行结果：

- 总数：94。
- 成功：92。
- 失败（failures）：0。
- 错误（errors）：1。
- 跳过（skipped）：1。
- 耗时：53.591 秒。
- 最终状态：`FAILED (errors=1, skipped=1)`。

跳过项：

- `test_e2e_production_smoke.ProductionChainSmokeTest.test_web_demo_production_chain`
- 原因：`LIFE_STEWARD_E2E is not enabled`。本次按要求将 `LIFE_STEWARD_E2E` 置空，因此真实生产链路 E2E 未执行，不能据此宣称真实 E2E 验收完成。

错误项：

- `test_database_integration.DatabaseIntegrationTestCase.test_vector_search_roundtrip`
- 错误发生在调用真实 Qwen/DashScope embedding 接口时：`openai.APIConnectionError: Connection error`，底层为 `httpx.ConnectError: [WinError 10013]`。
- 本轮受限执行环境禁止该外部网络请求；由于该测试会向外部服务发送测试记忆文本，未在没有额外明确授权的情况下绕过限制。

数据库测试是否仍卡住：否。

- 前 19 项通过后曾约 50 秒没有新增输出，但测试最终自行完成，并非永久卡死。
- `test_database_health_check_reports_connection_state`：通过。
- `test_initial_schema_tables_exist`：通过。
- `test_sql_tool_roundtrip`：通过。
- `test_user_profile_roundtrip`：通过。
- 向量回环未卡在数据库操作，而是在生成 embedding 的外部 HTTP 连接阶段报错退出。

原始完整输出：`backend/full_backend_regression_2026-08-26.log`。

## 前端验证方式

前端项目位于 `frontend` 目录。不要在 `backend` 目录执行 npm 命令，否则 npm 会查找不存在的 `backend/package.json` 并报 `ENOENT`。

```powershell
cd frontend
npm.cmd install                 # 首次执行时安装依赖
npm.cmd test                    # 契约测试
npm.cmd run build               # TypeScript 检查并构建 Vite 产物
```

也可以从仓库根目录直接执行：

```powershell
npm.cmd --prefix frontend test
npm.cmd --prefix frontend run build
```
