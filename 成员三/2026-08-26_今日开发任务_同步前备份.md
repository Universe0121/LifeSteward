# 成员三 2026-08-26 今日开发任务

负责人：成员三  
角色：Production API / Agent Workflow / E2E Owner  
基线：GitHub `origin/main@361cc37`

## 今日唯一 P0 目标

在成员二提供真实 DSN/key 后，完成正式 API、Agent 工作流和真实 E2E 收口，并为成员一的网页联通提供稳定接口。

## 一、10:30 前：后端回归和 planning 测试补齐

- 运行 GitHub 当前完整测试，记录 `90 tests` 基线。
- 检查 `backend/tests/test_master_agent_routing.py`：planning 路由当前存在空壳 `pass` 测试，必须补成真实断言。
- 断言必须覆盖：
  - `MemoryAgent -> PlanningAgent -> InteractionAgent` 顺序。
  - `generated_plan` 包含 `task_name`、`start_time`、`duration_minutes`、`difficulty`。
  - InteractionAgent 收到并生成最终用户响应。
- 不改变已冻结 `AgentState` 字段和公共 Chat API 字段。

## 二、13:00 前：正式 API 和错误语义收口

确认并测试：

- `GET /api/v1/life-events?user_id=10001&days=7` 正常、空结果、非法参数、Tool 异常均有明确行为。
- `record_event` 持久化失败返回标准错误，InteractionAgent 不执行，不出现成功文案。
- 真实数据库不可用时返回可定位的错误，不自动回退 `InMemoryMemoryService`。
- `/api/v1/chat`、`/api/v1/life-events` 都使用启动时共享的 production composition root。

如成员一发现字段问题，优先在 schema/API 适配，不让前端直接依赖数据库内部实现。

## 三、15:00 前：运行真实 Reflection/Planning E2E

等待成员二确认 DSN/key 后运行：

```powershell
cd backend
$env:LIFE_STEWARD_E2E = "1"
python -m unittest tests.test_e2e_production_smoke -v
```

必须证明：

- chat record 成功。
- `GET /api/v1/life-events` 读回成功。
- Reflection 返回 `intent=reflection`，检索记忆来自真实 pgvector。
- Planning 返回 `intent=planning`，计划字段完整。
- 真实 embedding 已使用；deterministic fallback 只能单独标为本地调试结果。

## 四、16:30 前：与成员一完成网页联调

- 提供 Chat API 和 life-events API 的稳定请求/响应样例。
- 协助成员一确认后端启动、代理、CORS/错误响应和用户 ID 一致。
- 用浏览器完成：聊天写入 → 时间轴回读 → Reflection → Planning。
- 如前端收到 500，确认错误响应不会被前端伪装成成功。

## 五、18:00 前：测试与 handoff

运行：

```powershell
cd backend
python -m unittest discover -s tests -p "test_*.py" -v
```

更新 `成员三/day06_handoff_03.md`，记录：

- 分支和 commit。
- API/schema/service 修改。
- planning 路由测试补齐情况。
- 持久化失败语义。
- 真实数据库、embedding、Reflection、Planning 和浏览器联调结果。
- 测试总数、跳过项及其原因。

## 今日禁止事项

- 不新增 Agent、AgentState 字段、模型提供商或数据库 Schema。
- 不为了测试全绿而删除真实环境 Gate。
- 不把 deterministic fallback 冒充 DashScope 真实验收。
- 不扩展 Profile、DIY、首页任务、Redis、通知或移动端。

## 完成标准

成员三只有在 API 测试、planning 路由断言、真实 Reflection/Planning E2E 和成员一浏览器联通全部通过后，今日任务才算完成。
