# 成员一 2026-08-26 今日开发任务

负责人：成员一  
角色：Frontend / Backend Integration Owner  
基线：GitHub `origin/main@361cc37`

## 今日唯一 P0 目标

今天必须完成网页端与后端代码的真实联通，并完成浏览器验收。不能只完成接口封装或静态页面，必须证明：

```text
浏览器 ChatHome
-> POST /api/v1/chat
-> FastAPI / MasterAgent
-> 返回 assistant_response / intent / extracted_events
```

以及：

```text
浏览器 Timeline
-> GET /api/v1/life-events?user_id=10001&days=7
-> PostgreSQL 真实事件
-> 页面显示刚刚聊天写入的事件
```

## 一、10:30 前：合并并确认联调基线

- 基于最新 `origin/main@361cc37` 工作，不使用旧分支覆盖后端。
- 确认 `frontend/src/api.ts` 的 `postChat()` 和 `getLifeEvents()` 指向正式 API。
- 确认 Vite `/api` 代理指向后端 `http://localhost:8000`。
- 生成当前会话稳定的 `conversation_id`，不要每次请求固定使用 `conv_001`。
- 记录后端启动地址、前端启动地址和当前 commit。

## 二、12:00 前：ChatHome 完成真实 API 联通

修改范围：`frontend/src/api.ts`、`frontend/src/pages/ChatHome.tsx`，必要时补充 contract test。

必须实现：

- 用户输入发送到 `POST /api/v1/chat`。
- 显示后端真实 `assistant_response`。
- 显示本轮 `intent`、`extracted_events`、`conversation_id` 和请求状态。
- 请求中 loading，发送按钮禁用，空输入不提交。
- 请求失败显示真实错误和“重试”，不得显示：
  - “我先帮你记下了”
  - “演示模式：已使用 mock 回复”
  - 任何暗示数据库已写入的假成功文案。
- 后端返回 500 时，页面必须保持可继续操作。

## 三、14:00 前：Timeline 完成真实 API 联通

修改范围：`frontend/src/pages/Timeline.tsx`，只使用后端 `getLifeEvents()`。

必须实现：

- 首次进入请求 `GET /api/v1/life-events?user_id=10001&days=7`。
- 显示 loading、空数据、错误、重试四种状态。
- 不再默认读取 `mocks/timeline_events.json`。
- 聊天页写入事件后，切换到时间轴页面能够读到同一条真实记录。
- 保留日期选择、类型筛选和展开详情，但不新增页面。

## 四、15:30 前：本地浏览器四步验收

启动顺序：

```powershell
cd backend
python -m uvicorn main:app --reload

cd frontend
pnpm run dev
```

浏览器必须按顺序完成：

1. ChatHome 输入“今天学习数学2小时，有点累”。
2. 页面显示后端 assistant response 和 `record_event`。
3. 打开 Timeline，读回相同的真实事件。
4. 回到 ChatHome，输入 Reflection 和 Planning 问题并看到后端响应。

另做一次错误态验收：暂时停止后端，发送消息，确认页面显示错误而不是 mock 成功。

## 五、17:00 前：构建和交接

运行：

```powershell
cd frontend
node --test tests/contract.test.mjs
pnpm run build
```

交付：

- 浏览器四步验收结果和截图/录屏。
- Chat API、life-events API 的实际请求和响应摘要。
- 前端构建结果。
- 更新 `成员一/day06_handoff_01.md`，记录分支、commit、修改文件、真实联通状态、错误态验证和未完成问题。

## 今日禁止事项

- 不新增页面、不重做视觉、不做移动端。
- 不把 Profile、DIY、首页任务改为后端数据。
- 不在前端保存 DSN、DashScope key 或数据库错误堆栈。
- 不用 mock 响应掩盖真实后端失败。

## 完成标准

成员一只有在“浏览器发送真实 Chat 请求 + Timeline 读回真实事件 + 后端失败显示真实错误 + 前端 build 通过”全部完成后，今日任务才算完成。
