# 成员二 2026-08-26 今日开发任务

负责人：成员二  
角色：PostgreSQL / pgvector / Real Evidence Owner  
基线：GitHub `origin/main@361cc37`

## 今日唯一 P0 目标

解除真实数据库和真实 embedding 阻塞，为成员一网页联通、成员三 E2E 提供可复现环境和脱敏证据。

## 一、10:00 前：环境和 migration Gate

- 配置本地 `backend/.env`：`POSTGRES_DSN`、`DASHSCOPE_API_KEY`、`EMBEDDING_MODEL_NAME`。
- 真实值不得提交到 GitHub。
- 执行 `backend/migrations/001_initial_memory_schema.sql`。
- 健康检查必须显示：
  - `connected=True`
  - `vector_extension_available=True`
- 确认七张表存在：`life_events`、`memories`、`user_profile`、`goals`、`plans`、`feedbacks`、`reflections`。

如果 10:00 仍无 DSN 或 key，必须立即报告外部阻塞；deterministic fallback 只能用于本地调试，不能写入最终验收结论。

## 二、11:30 前：数据库 5 项 Gate 实跑

运行：

```powershell
cd backend
python -m unittest tests.test_database_integration -v
```

必须 5/5 实际执行并通过：

- PostgreSQL/pgvector health check。
- 七张表存在检查。
- `SQLTool.save_life_events()` / `get_recent_events()` round-trip。
- `update_user_profile()` round-trip。
- 真实 `LLMService.embed_text()` + `VectorSearchTool` round-trip，包含 `memory_id`、`memory_content`、`similarity_score` 和真实向量维度。

## 三、14:00 前：提供网页联调数据

通过正式 `POST /api/v1/chat` 写入 `user_id=10001`：

- 最近三天每天只睡5小时。
- 最近学习效率很差。
- 压力比较大。

随后验证：

- `life_events` 有对应真实记录。
- `memories.embedding` 非空。
- `GET /api/v1/life-events` 能读回记录。
- 查询“最近为什么学习效率下降？”能返回真实相似记忆。

不得直接用 SQL 插入来替代正式聊天链路。

## 四、16:00 前：向成员一、成员三交付

交付脱敏信息：

- DSN 是否可用，不发送密码。
- migration 和 pgvector 状态。
- embedding 模型名和维度。
- 事件数量、记忆数量、相似度结果字段样例。
- 数据库测试 `5/5` 输出摘要。

## 五、17:00 前：安全清理和 handoff

- 仅清理 `user_id=10001` 演示数据，不得 `TRUNCATE` 或清空整库。
- 保留可重复启动、migration、测试和清理命令。
- 更新 `成员二/day06_handoff_02.md`，记录分支、commit、真实环境结果、测试数量、证据和剩余阻塞。

## 今日禁止事项

- 不做 Redis、监控、数据库重构、新表、新 migration 设计。
- 不修改 React、Agent、Prompt。
- 不把固定向量或 deterministic fallback 写成真实生产验收证据。

## 完成标准

数据库 5 项 Gate 实跑通过，并且成员一可以用网页聊天写入后在 Timeline 读到真实数据，成员三可以在同一环境运行 Reflection/Planning E2E。
