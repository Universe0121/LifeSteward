# 开发规范检查记录

| 检查项 | 结果 | 说明 |
|---|---|---|
| Day1 页面范围 | 通过 | 仅实现聊天、时间轴、画像三页 |
| 技术栈 | 通过 | React + TypeScript + Vite |
| 样式约束 | 通过 | 纯 CSS，未引入 UI 框架 |
| API 字段 | 通过 | `user_id`、`conversation_id`、`user_input`、`assistant_response`、`extracted_events` 保持不变 |
| 命名规范 | 通过 | 接口和 mock 数据使用 `snake_case` |
| 分层约束 | 通过 | 前端只调用 HTTP，不直连 LLM 或 Database |
| AgentState | 通过 | 前端只展示后端返回数据，不创建新的 Agent 间传输协议 |
| 后端改动 | 通过 | 本轮未修改后端业务代码 |
| 超出范围功能 | 通过 | 未实现 Memory、Reflection、Planning、RAG、通知和移动端 |
| 构建验证 | 待环境 | 当前机器未检测到 Node/npm，无法执行 Vite build |
